import React from 'react';
import './Header.scss';

const Header = () => (
  <header className="top-header">
    <h1 className="top-header__title">The Todoer</h1>
    <p className="top-header__description">The number one Todo App in town!</p>
  </header>
);

export default Header;
